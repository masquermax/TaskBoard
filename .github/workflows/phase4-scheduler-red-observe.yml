name: Phase 4 Scheduler RED observe

on:
  push:
    branches: [agent/completion-single-owner-phase4]
    paths:
      - .github/workflows/phase4-scheduler-red-observe.yml
      - tests/completion-scheduler-phase4.test.js
      - src/core/scheduler.js

permissions:
  contents: read

jobs:
  observe-scheduler-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Run focused Scheduler projection test
        id: focused
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          node --test tests/completion-scheduler-phase4.test.js 2>&1 | tee phase4-scheduler-red.log
      - uses: actions/upload-artifact@v4
        with:
          name: phase4-scheduler-red-evidence
          path: phase4-scheduler-red.log
          if-no-files-found: error
      - name: Preserve RED status
        if: steps.focused.outcome == 'failure'
        run: exit 1
