name: Apply bounded planning repair patch

on:
  push:
    branches: [agent/root-goal-contribution-witness]
    paths: [.github/workflows/apply-bounded-planning-repair.yml]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/root-goal-contribution-witness
      - run: python scripts/apply-bounded-planning-repair.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/apply-bounded-planning-repair.yml scripts/apply-bounded-planning-repair.py
          git add src/core/root-runtime.js
          git diff --cached --check
          git commit -m "fix: bound Root planning contract repair"
          git push origin HEAD:agent/root-goal-contribution-witness
