{
  "name": "taskboard-codex",
  "version": "0.9.2",
  "private": true,
  "type": "module",
  "description": "Local-first AI task board with Human Gateway and a decoupled Codex Executor.",
  "scripts": {
    "dev": "node --watch src/server/mock.js",
    "start": "node src/server/index.js",
    "start:mock": "node src/server/mock.js",
    "test": "node --test",
    "check": "node scripts/check-syntax.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=16.6.0"
  }
}
